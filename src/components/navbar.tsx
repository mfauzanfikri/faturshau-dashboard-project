"use client";

import { Navbar as Nav } from "flowbite-react";

const Navbar = () => {
  return (
    <Nav fluid>
      <Nav.Brand>
        <p>Faturshau Dashboard</p>
      </Nav.Brand>
    </Nav>
  );
};

export default Navbar;
